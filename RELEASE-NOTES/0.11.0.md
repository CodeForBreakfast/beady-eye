bdi 0.11.0

Minor release, **0.10.2 → 0.11.0**.

## Highlights

**A bead's row is configurable.** A `[row]` table in the config says which cells the row draws and in what order. Nothing to do: without one the row is drawn as it always has been, and `docs/configuration.md` has the table.

## Maintenance

Internal improvements.

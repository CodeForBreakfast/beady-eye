bdi 0.10.1

Patch release, **0.10.0 → 0.10.1**.

## Highlights

**Folding a large forest is far faster, and moving through it no longer lags.** `E`, `C`, `e`, `c` and `d` settle in one draw however deeply the folds nest, and a key that only moves the selection draws nothing, so the keystrokes after a big expand answer at once. Nothing to do; every key does what it did.

## Maintenance

More tests around the fold keys.

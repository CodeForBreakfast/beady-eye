[
  {
    "id": "bdi-7ao.22",
    "title": "Run bdi under a file watcher so a running copy picks up changes",
    "description": "Graeme keeps a `bdi` running in a terminal while he works, and the main checkout\npicks up changes from other seats. He wants the running copy to be current without\nquitting and restarting it by hand, and he wants starting it that way to be one\nshort command rather than a line he has to remember.\n\nCargo has nothing built in for this — no `cargo run --watch` — so it takes a\nwatcher in the dev shell. All three candidates are in this flake's pinned nixpkgs,\neval'd from `flake.lock` by this seat on 2026-08-31:\n\n- watchexec 2.5.1 — general-purpose, has restart-on-change\n- cargo-watch 8.5.3 — the `cargo watch -x run` subcommand\n- bacon 3.25.0 — draws its own TUI, so it is the wrong shape for hosting one\n\nwatchexec is the pick. The shape of the command:\n\n    watchexec -w src -w Cargo.toml -r -- cargo run\n\n## Making it one word\n\nThe choice of watcher decides what the shortcut can be, so decide the watcher\nfirst. A `[alias]` in .cargo/config.toml only maps to cargo subcommands, so it\nreaches cargo-watch and cannot reach watchexec. Neither .cargo/ nor scripts/ exists\nin this repo at `40af2eb`, so nothing here sets a precedent.\n\nWith watchexec that leaves two:\n\n- `pkgs.writeShellScriptBin` in the flake's devShell. Lands on PATH inside direnv,\n  no new directory, no chmod, invoked bare — and it comes from the same flake that\n  supplies watchexec, so the tool and the command that needs it cannot drift apart.\n- A `scripts/watch` file. Plainer to read and edit, but has to be found and kept\n  executable, and says nothing about needing watchexec.\n\nThe first is the recommendation. Name it for what it does.\n\n## Blocked by bdi-7ao.21\n\nwatchexec restarts by killing the running bdi, and until bdi restores the terminal\non a signal every rebuild leaves the shell in raw mode with the alternate screen up.\nLanding this before that fix hands Graeme a trap rather than a convenience.\n\n## Done when\n\n- The dev shell carries the watcher, added to `rustTools` in flake.nix (`:57` at\n  `40af2eb`, feeding both `buildInputs` at `:80` and `:97`) so both the default and\n  maintainer shells get it.\n- One bare command starts bdi under it, on PATH from the dev shell.\n- CLAUDE.md's \"Working in this repo\" names that command. The command carries the\n  flags; the prose does not repeat them.\n- It is run once and seen to work: change a file in src/, watch bdi rebuild and come\n  back, and confirm the terminal is intact afterwards.\n- The watch paths are checked against what actually matters. Watching all of src/\n  rebuilds on a test-only edit too, which may or may not be wanted.\n",
    "status": "in_progress",
    "priority": 1,
    "issue_type": "task",
    "assignee": "Graeme Foster",
    "owner": "80714+GraemeF@users.noreply.github.com",
    "created_at": "2026-08-31T07:55:38Z",
    "created_by": "Graeme Foster",
    "updated_at": "2026-08-31T09:05:09Z",
    "started_at": "2026-08-31T09:05:08Z",
    "metadata": {
      "agent_pane": "wD6:pJ",
      "working_topic": "beady-eye/v1-bdi-7ao.22"
    },
    "dependencies": [
      {
        "issue_id": "bdi-7ao.22",
        "depends_on_id": "bdi-7ao",
        "type": "parent-child",
        "created_at": "2026-08-31T07:55:38Z",
        "created_by": "Graeme Foster",
        "metadata": "{}"
      },
      {
        "issue_id": "bdi-7ao.22",
        "depends_on_id": "bdi-7ao.21",
        "type": "blocks",
        "created_at": "2026-08-31T07:55:41Z",
        "created_by": "Graeme Foster",
        "metadata": "{}"
      }
    ],
    "dependency_count": 1,
    "dependent_count": 0,
    "comment_count": 0,
    "parent": "bdi-7ao"
  },
  {
    "id": "bdi-7ao.21",
    "title": "A signal kills bdi without restoring the terminal",
    "description": "bdi restores the terminal from `Drop` on `Screen` (src/tui.rs:784). That runs when\nthe loop returns normally — 'q' — and on a panic, because `ratatui::init` hooks\npanics too. Neither Cargo.toml nor flake.nix sets a `[profile]`, so both builds\nunwind and that panic path is real. Nothing in src/ installs a signal handler;\n`signal-hook` is in Cargo.lock only as crossterm's resize plumbing.\n\nA signal with no handler terminates the process outright: no unwind, no `Drop`, no\n`ratatui::restore()`. So anything that kills bdi from outside — herdr reclaiming a\npane, a `pkill`, a watcher restarting it after a rebuild — leaves the terminal in\nraw mode with the alternate screen up and mouse capture on.\n\nMeasured on 2026-08-31, on a pty, for SIGTERM, SIGINT and SIGHUP alike. Each one\nends the process with *terminated by signal* and zero bytes written after it. What\nbdi turns on at startup, and what each signal therefore leaves on:\n\n    ?1049h  ?1000h  ?1002h  ?1003h  ?1015h  ?1006h  ?25l\n\nThe control matters as much as the fault: quitting with 'q' emits every one of them\nback — `?1006l ?1015l ?1003l ?1002l ?1000l ?1049l ?25h` — in the reverse of the\norder they were set. So `Drop` already restores the complete set, and the fix needs\nno mode list of its own. One that grew one would go stale the first time crossterm\nchanged what `EnableMouseCapture` sets.\n\nThe mouse modes are the ones that hurt. A terminal still reporting the mouse writes\nan escape sequence into whatever runs next for every cell the pointer crosses, which\nreaches the reader as phantom keystrokes typing themselves. Recovering a terminal\nleft this way needs the mouse turned off before the rest:\n\n    printf '\\033[?1003l\\033[?1006l\\033[?1002l\\033[?1000l'; stty sane; tput rmcup\n\nThe first collection runs before the alternate screen is entered — `tui::run` calls\n`collect(&Wanted::Everything)` ahead of `Screen::showing` — and took 5.0–5.5 s on a\ndebug build against the real trackers, 140 bytes of output before `?1049h`, all of\nthem `inbound`'s notice about a second bdi. A signal in that window is harmless,\nbecause there is nothing to restore yet, and measurably still kills outright.\n\nThis is what a test has to be written around: a pty test that sleeps a fixed time\nshorter than the collection signals a bdi that has not drawn anything, sees no\nrestore sequences either way, and passes against a broken build. Wait for `?1049h`\nin the output, never for a clock.\n\n## Decisions taken here\n\n**Where the signals are taken, and why not earlier.** `run` registers them between\nthe first collection and `Screen::showing`, so they are ours before anything is\nasked of the terminal and the only unhandled window is one where nothing needs\nrestoring. Registering ahead of the collection would be worse rather than better: a\nsignal there would then be answered by finishing a five-second collection, opening\nthe screen and closing it again, where dying on the spot costs the reader nothing.\nMeasured both ways.\n\n**The event is its own variant, not a synthesised keystroke.** `drive`'s first arm\nanswers any key at all by taking the bindings window away, so a fake 'q' arriving\nwith that window up would close the window and leave bdi running.\n\n**No mode list in the signal path.** `Drop` already emits all six modes and the\ncursor, proved by the 'q' control above. A hand-written list would go stale the day\ncrossterm changed what `EnableMouseCapture` sets.\n\n**Ctrl-C is not SIGINT here.** Raw mode clears `ISIG`, so Ctrl-C reaches bdi as a\nkey event and SIGINT only ever arrives from an external `kill -INT`. Anyone\nverifying this fix by pressing Ctrl-C will see behaviour unchanged, and that is\ncorrect rather than a regression.\n\nWhat surfaced it: Graeme wants bdi running in a terminal that picks up changes to\nthe main checkout, which means `watchexec -w src -r -- cargo run` (bdi-7ao.22, which\nthis blocks). watchexec has to kill the running bdi to restart it, so as things\nstand every rebuild would wreck the terminal. Wrapping the command in a shell that\ntraps EXIT and resets is a stopgap that avoids the code change, not the fix.\n\n## Done when\n\n- SIGTERM, SIGINT and SIGHUP each end the run through the normal path, so the `Drop`\n  that restores the terminal runs.\n- The restore is proved rather than argued: a test drives bdi on a pty, waits for\n  the alternate screen, signals it, and asserts the leave-alternate-screen and\n  mouse-off sequences are emitted.\n- The dependency this needs is named.\n- The comment on `Drop` at src/tui.rs:784 says a `panic = \"abort\"` profile would\n  take the restore with it. Once signals route through `Drop` that clause covers\n  three paths — normal return, panic and signal — and it currently reads as\n  covering one.",
    "status": "closed",
    "priority": 1,
    "issue_type": "bug",
    "assignee": "Graeme Foster",
    "owner": "80714+GraemeF@users.noreply.github.com",
    "created_at": "2026-08-31T07:51:29Z",
    "created_by": "Graeme Foster",
    "updated_at": "2026-08-31T09:03:14Z",
    "started_at": "2026-08-31T08:31:27Z",
    "closed_at": "2026-08-31T09:03:09Z",
    "close_reason": "Fixed and measured. SIGHUP, SIGINT and SIGTERM are now taken in run() before the screen opens and answered on a thread of their own, which sends Event::Signalled into the loop; the loop returns, Screen's Drop runs, and the terminal is handed back exactly as it is on 'q'. Confirmed on a real pty: before the fix all three signals left ?1049h, ?1000h, ?1002h, ?1003h, ?1015h, ?1006h and ?25l set with zero bytes written after the signal; after it, all three restore. Three pty tests in tests/signal_restores_the_terminal.rs and two unit tests in src/tui.rs guard it. Mutation-tested with --in-diff, no survivors. Landed direct to main as 79d061c1fef584f6f78848bf99740f7ba01df08c; CI run 33375192748 completed with conclusion success on that headSha.",
    "dependencies": [
      {
        "issue_id": "bdi-7ao.21",
        "depends_on_id": "bdi-7ao",
        "type": "parent-child",
        "created_at": "2026-08-31T07:51:29Z",
        "created_by": "Graeme Foster",
        "metadata": "{}"
      }
    ],
    "dependency_count": 0,
    "dependent_count": 2,
    "comment_count": 0,
    "parent": "bdi-7ao"
  },
  {
    "id": "bdi-7ao",
    "title": "bdi v1: a tree of work in flight",
    "description": "`bdi` joins a beads tracker to a herdr session and draws one tree of work per\nroot, annotated with the live agent on each node. Read-only. This epic is the\nwhole of what `docs/design.md` calls v1.\n\n**`docs/design.md` is a starting point, not gospel** — where it is ambiguous the\nseat that hits the ambiguity decides with the code in front of it and records\nwhat it chose, and `bdi-2bb.1` folds those in. Graeme's decision, 2026-08-30;\nthe fuller statement is below. `CLAUDE.md` carried the superseded \"accepted\nspec\" line until `bdi-8df` took it out; that line had misrouted three seats in\na day, the last of which staged a finished P1 and refused to push.\n\n**Effort slug: `v1`.** Every commy topic for this effort is built from it —\n`#beady-eye/v1`, `#beady-eye/v1-decisions`, `#beady-eye/v1-<bead>`. A\nreplacement orchestrator must reuse this slug; inventing a fresh one posts into\ntopics no worker is subscribed to and registers a second `#general/fleet` line\nfor an effort that already has one.\n\n**Delivery mode: direct, for this repo and it is the only repo.** Measured\n2026-08-30: `gh api repos/CodeForBreakfast/beady-eye/rules/branches/main`\nreturns 403 *\"Upgrade to GitHub Pro or make this repository public\"*, and\n`branches/main` reports `.protected: false`. Branch protection is not merely\nunused here — it is unavailable on a private repo on the free plan. So workers\ncommit and push to `main`, beads close on push, and there is no PR cap and no\ndelivery ordering. That changes the day the repo goes public (bdi-iby), which\nis exactly what bdi-fjo covers.\n\n---\n\n## Two halves, deliberately sequenced\n\n1. **Core and `--json`** — `bdi-3um`, **done**, landed at `86cd807`. Config,\n   both collectors, tree construction, the join, anomaly rules, failure\n   classification, the snapshot and the CLI, up to and including the published\n   JSON contract. 167 tests, clippy and fmt clean.\n2. **The TUI** — `bdi-2bb`, the interface the name promises. Planned second and\n   written against the real types, because a plan drafted against imagined ones\n   is wrong the way the core's plan was: its task listing was wrong in all\n   eleven tasks, and every seat that ran one found a real defect in its brief.\n\n**v1 is done when** `bdi` with no flags draws the forest (`bdi-2bb.7`), it\nworks in a tracked repo with no config file (`bdi-wf5`), and CI runs the flake\nchecks on every push (`bdi-1a6`). All three are closed. **Release is not a v1\nclause.** Graeme moved `bdi-7ph` under `bdi-iby` on 2026-08-31: a tag\npublishing to crates.io and Nix needs a licence and a public repo, so it\nbelongs to going public. The TUI is built and runnable: `bdi-2bb.5`, `.6` and `.7` are\nclosed and there is no not-built exit path left in `main.rs` or `app.rs`, so a\nseat that needs to see a real screen can render one. `bdi-vx7` closed on 2026-08-31 — it was worked, not deferred, and it is what\nturned `bdi-7ao.13` and `.14` from design opinion into measurement.\n`bdi-2bb.1` reconciles the design document after the rest lands.\n\n**Explicitly not v1** (`docs/design.md`, *Non-goals for v1*): no writes to bd\nbeyond `herdr agent focus`, no Noctalia widget until the TUI has proved the\ndata model, no cross-machine view.\n\n**Going public is not here.** That is `bdi-iby` — the licence, the scrub of\nprivate infrastructure from tracked files, the Dolt host still in git history\n(`bdi-a8s`), the PR gate, and **the release itself (`bdi-7ph`)**. Packaging into the NixOS config is tracked in that project as `nix-b8et4`.\n\n---\n\n## Roster\n\nEverything below is in this project's tracker, and **no strand is worked by another project, so there are no tracking beads.**\nOne carries outbound coordination the orchestrator owns rather than work it\ncan staff: `bdi-7ph` points at `nix-b8et4` in the Summit-works project for the\nconsuming side of packaging. It goes through commy to that project's channel,\nand it now lives under `bdi-iby`, not here.\n\n**The TUI (`bdi-2bb`), seven children:**\n\n**`.2` to `.9` are the original build and are closed.** This table is a reading\nof the tracker, not the tracker: beads found mid-effort are added here as they\nare filed, and `bd list --limit 0` is what a cold orchestrator should trust.\n\n| Bead | What |\n|---|---|\n| `bdi-2bb.2` | Read and focus a herdr pane |\n| `bdi-2bb.3` | The failure vocabulary, and the view module scaffolding |\n| `bdi-2bb.4` | Flatten a snapshot into a navigable forest |\n| `bdi-2bb.5` | Draw the forest with ratatui |\n| `bdi-2bb.6` | Run the loop, and the refresh-source seam with a timer behind it |\n| `bdi-2bb.7` | Tail the selected pane, and focus it on ⏎ |\n| `bdi-2bb.8` | Let any process tell `bdi` a project changed |\n| `bdi-2bb.9` | Let the live-agent filter be re-applied without re-collecting |\n| `bdi-2bb.21` | Say when a collection is running, and when the tree on screen was collected |\n| `bdi-2bb.22` | A blocker and a child are drawn the same, though the row now knows which it is |\n| `bdi-2bb.23` | The 30-second refresh interval rests on a collection cost `0f8aeae` changed |\n| `bdi-2bb.1` | Bring `docs/design.md` in line with the TUI that got built — runs **last** |\n\n**Everything else `bdi` needs:**\n\n| Bead | What |\n|---|---|\n| `bdi-wf5` | `bdi` with no config should work from the current directory |\n| `bdi-ijq` | `roots.explicit` and `bdi <bead-id>` carry no project scope |\n| `bdi-z0d` | flake checks, and the two flake defects |\n| `bdi-1a6` | CI on push: run the flake checks in GitHub Actions |\n| `bdi-vx7` | Prove the per-project credential against a live second tracker — **closed 2026-08-31.** It ran against beady-eye and summit-works together and failed twice, which is where `bdi-7ao.13` (`BEADS_DIR` inherited) and `bdi-7ao.14` (`direnv exec` over restated config) came from. |\n\n---\n\n## Critical path\n\n```\nbdi-2bb.9 → .4 ─┐\nbdi-2bb.3 → .5 ─┼→ .7 ─┐\n         └→ .6 ─┘      ├→ bdi-2bb.1\nbdi-wf5 → bdi-ijq → .6 │\nbdi-2bb.2 ──────→ .7 ──┘\n              .6 → .8 ─┘\n```\n\n`bdi-2bb.3` is the head of the TUI: it lands the module skeleton with real\nsignatures — every module created and declared in `src/view/mod.rs`, plus the\ndomain-action enum — so nothing after it edits a file another bead is holding.\nThat is what lets `.4`, `.5` and `.6` all run at once. `bdi-2bb.9` needs\nnothing and can start immediately beside it.\n\n`bdi-z0d → bdi-1a6` ran alongside and is closed. `bdi-7ph` continued it and\nhas moved to `bdi-iby`.\n\nThe live critical path is now `bdi-2bb.21` and `bdi-2bb.22` → `bdi-2bb.1` →\n`bdi-2bb` → the epic. The tracker-resolution epic `bdi-7ao.15` (`.13`, `.14`)\nruns alongside and touches `config.rs` and the bd runner, not the view.\n\n---\n\n## File ownership — seats share one working tree\n\nThere are no worktrees here; concurrent seats edit the same checkout, so two\nbeads owning one file is a collision, not a merge. This map is why several\nedges above exist:\n\n| File | Owner |\n|---|---|\n| `Cargo.toml`, `Cargo.lock`, `src/lib.rs` | `bdi-2bb.3` **only** — any bead wanting a dependency or a module routes back here |\n| `src/main.rs` | `bdi-wf5` → `bdi-ijq` → `bdi-2bb.6`, in that order |\n| `src/config.rs` | `bdi-wf5`, then `bdi-2bb.6` |\n| `src/model/snapshot.rs` | `bdi-2bb.9` — **and no one else**; `bdi-2bb.4` and `bdi-ijq` were both drawn toward it and were re-shaped to stay out |\n| `src/app.rs` | `bdi-wf5`, then `bdi-ijq` |\n| `src/collect/herdr.rs`, `src/collect/run.rs` | `bdi-2bb.2` |\n| `src/view/mod.rs`, `src/view/row.rs` | `bdi-2bb.3` |\n| `src/view/forest.rs` | `bdi-2bb.4` |\n| `src/view/tail.rs` | `bdi-2bb.7` — created and declared as a stub by `bdi-2bb.3` |\n| `src/view/draw.rs` | `bdi-2bb.5`, then `bdi-2bb.7` at its stub points |\n| `src/tui.rs` | `bdi-2bb.6`, then `bdi-2bb.7` at its stub points |\n| `flake.nix` | `bdi-z0d` |\n| `.github/` | `bdi-1a6` |\n| `docs/design.md`, `CLAUDE.md` | `bdi-2bb.1`, and `bdi-2bb.8` for the protocol. `bdi-8df` borrows `CLAUDE.md` for one clause and hands it back |\n| `README.md` | `bdi-2bb.8` |\n\n`bdi-2bb.7`'s two edits into `draw.rs` and `tui.rs` are the only files any bead\nshares with another, which is why it is wired behind both rather than beside\nthem. Everything not listed here — `tests/`, `docs/plans/` — is unowned, so a\nseat needing one should say so rather than assume it is free.\n\n**These seven children are the TUI's plan.** `CLAUDE.md` says the TUI \"gets its\nown plan, written against the real types\"; this is it. There is no\n`docs/plans/` document for it and none is expected — the core's plan was wrong\nin all eleven of its task listings, and writing the next one as beads against\nthe landed types is the correction.\n\n---\n\n## Constraints every seat inherits, proved on the core\n\n- **No bd or herdr error text reaches the screen verbatim.** bd's auth failure\n  names the database and the SQL user. The model carries a failure *kind*; the\n  renderer says the words. `bdi-2bb.3` is where those words get written.\n- **Test data is fictional.** `tests/fixtures/` stays a faithful capture of\n  this machine's own session; everything inline is invented. `bdi-ifn` scrubbed\n  83 real identifiers out of the core's tests — don't put them back.\n- **Mutation-test before trusting a green.** Every seat on the core found a\n  real hole this way. `cargo-mutants` is not in the dev shell but *is* reachable\n  with no flake change — `nix develop --command nix shell nixpkgs#cargo-mutants\n  --command cargo-mutants mutants --file <path>`, nested so the toolchain still\n  comes from the dev shell. Note the `mutants` subcommand: without it the binary\n  prints cargo's usage, which reads exactly like \"not installed\". Hand-applying\n  a chosen mutant is still the right tool when you have a specific property in\n  mind; a sweep is for finding what you did not think of. `bdi-7ao.11` holds the\n  open question of pinning it and whether a sweep becomes a gate.\n- **Run the mutation harness under a memory ceiling**, because a deleted cycle\n  guard is a plausible mutant and an unbounded loop took 15.7 GiB in under two\n  minutes:\n  `systemd-run --user --scope -p MemoryMax=4G -p MemorySwapMax=0 -q -- cargo test …`\n  `MemorySwapMax=0` is load-bearing — with 68 GiB of swap, `MemoryMax` alone\n  thrashes instead of killing. Do **not** use `ulimit -v`: it caps address\n  space, which Rust reserves per thread, so it produces false kills that read\n  exactly like killed mutants.\n- **Question the brief.** Every seat on the core found its task listing wrong.\n  A brief that does not match the code is a defect in the brief.\n- **An unlanded branch is `wip_branch` / `wip_commit` metadata on the bead**,\n  never a note. Metadata is single-valued, so there is one answer and it is the\n  current one; appended notes leave a stale SHA standing above a fresh one, and\n  a top-down reader hits the stale one first. It also makes the work findable\n  from cold: `bd list --has-metadata-key wip_branch --limit 0 --json`.\n\n---\n\n## Two decisions Graeme made on 2026-08-30, mid-prep\n\n**1. `docs/design.md` is a starting point, not gospel.** We deviate as we learn\nand get into the details. This supersedes `CLAUDE.md`'s line that the design\n*\"is accepted; changes to it are design decisions, not edits.\"* `bdi-8df`\nreplaced that line with this rule, so the repo and the anchor now say the same\nthing. In practice: where the design is ambiguous, the seat that hits the\nambiguity decides it with the code in front of it and records what it chose;\n`bdi-2bb.1` folds those into the document at the end. Every such question is\nalready written into the bead that owns it, with a recommended default.\n\nIts known drift, for the same bead: the JSON example (~lines 451–476) shows\n`\"anomaly\": null` where the model emits `anomalies: []`, and its `unattributed`\nentry lacks the `project` field the model carries. The prose governs and the\ncode already follows it.\n\n**2. Refresh is a capability, not a fixed poll.** The design says refresh is a\npoll *because herdr has no event stream* — true of herdr, not of bd. So `bdi`\ndetects a change-notification source per project, uses it where one exists, and\nfalls back to a timer where none does. `bdi-2bb.6` builds the seam and the\ntimer; `bdi-2bb.8` adds the source.\n\n**And the source is an interface, not a built-in watcher** — any process can\ntell `bdi` a project changed, and a setup wires whatever it has. That holds the\nline this project was designed under: `bdi` knows nothing about how a setup is\norganised. It is also forced by the facts. The assistant project's mechanism\n(`#assistant/bd-change-notification`, 2026-08-30) is Dolt's binlog consumed as\na GTID stream, which needs a replication user and a unique `server_id` —\nserver-level privileges beady-eye's DB-scoped tenant user does not have and\nshould not require of anyone else — and Dolt multiplexes every tenant onto one\nstream, so a built-in consumer would receive row data for trackers the user\nnever asked to watch. Behind a socket, that consumer needs nothing from `bdi`\nand `bdi` needs nothing from the cluster.\n---\n\n## Seat roster, 2026-08-31 10:00 BST\n\nRead `agent_pane` from the tracker for who is live. `bd ready` is now honest:\nevery v1 strand is either staffed or held by a real dep edge, so what it lists\nis genuinely spawnable.\n\n| bead | state |\n|---|---|\n| `bdi-7ao.14` `.16` `.17` `.18` `.19` `.25`, `bdi-2bb.25` | **closed**, all green. `bdi-2bb.25` landed `ca856fe`+`40f4eb5`: `Header` is gone, a project owns its own line, roots are ordinary bead rows |\n| `bdi-7ao.21` | **P1, live.** Signal fault measured and confirmed. Designing the fix |\n| `bdi-n1m` | live. `CLAUDE.md` landed as `40af2eb`; charter pointer next |\n| `bdi-2bb.21` | live, **respawn** — the first seat went dark at 75 minutes with no commit and no post. Working topic is now `v1-bdi-2bb.21-r2`; the original topic is retired |\n| `bdi-7ao.24` | live. Correcting six beads' descriptions against the landed types |\n\n**Workers close when done and new work gets a fresh seat.** Graeme's rule,\n2026-08-31. A fresh seat arrives cold and audits its brief; a warm one reads the\nbrief through what it already believes. Do not stack beads on a productive\nworker.\n\n## What Graeme asked for, and the chain that delivers it\n\n`bdi-7ao.22` — run `bdi` under a watcher so a running copy picks up changes — is\n**P1 and is what he actually wants**. It is dep-blocked on `bdi-7ao.21`, and the\nreason is written into it: watchexec restarts by killing `bdi`, so until a signal\nrestores the terminal every rebuild hands him a trap. So `.21` is the critical\npath, not a P2 bug.\n\n**The signal fault is measured, not inferred.** SIGTERM, SIGINT and SIGHUP each\nleave six DEC private modes set — `?1049h ?1000h ?1002h ?1003h ?1015h ?1006h` —\nplus the cursor hidden, with **zero bytes written after the signal**. Quitting\nwith `q` emits the exact reverse. So `Drop` already restores the complete set:\n**the fix routes signals through `Drop` and must not grow its own mode list**,\nwhich would rot when crossterm changes what `EnableMouseCapture` sets.\n\nRecovery for a terminal left in that state:\n`printf '\\033[?1006l\\033[?1015l\\033[?1003l\\033[?1002l\\033[?1000l\\033[?25h'; stty sane; tput rmcup`\n\n**`bdi-7ao.26` — the TUI never clears on startup — is independent by\nconstruction and must not be closed as a duplicate of `.21`.** Measured: after a\n**SIGKILL** (uncatchable, so no signal handling can prevent it) the next run\nemits no erase sequence at all and leaves **1612 of 4800 cells untouched, 33.6%**,\non a 120x40 screen. Stale text survives in the gaps, which is why it reads as\nintermittent. One `terminal.clear()` after init in `Screen::showing`\n(`src/tui.rs:735`) fixes it permanently. Held behind `.21` for landing order in\nthe same function, not because the bug depends on it.\n\n## Dependency edges added 2026-08-31 10:00, and why\n\n`bdi-sw4`, `bdi-7ao.20`, `bdi-2bb.22`, `bdi-2bb.24`, `bdi-7ao.23` and\n`bdi-7ao.12` are all **blocked by `bdi-7ao.24`**, which is rewriting their\ndescriptions right now against the landed row types. Spawning onto a bead whose\ndescription is mid-correction is how a seat builds from a stale premise — which\nnearly retired three beads today. They release when `.24` closes.\n\n**`bdi-sw4` is NOT settled by the split.** The `bdi-2bb.25` seat claimed it was\nand withdrew the claim. Its premise is a colour measurement on Graeme's\nterminal: `themes/noctalia.conf` sets `foreground` and `color15` to the same\n`#ede0da`, so `\\e[38;5;15m` and `\\e[39m` collapse exactly. Nothing in the split\nchanges which SGR codes are emitted. A staffed root now sits on that scale too,\nso the bug has **one more row on it** than before.\n\n**`bdi-7ao.24` is the pass over the parked beads**, blocked by `bdi-2bb.25`.\nTheir descriptions do not go stale gradually — they become wrong at that one\nsha. Do not spawn it early; the dep edge holds it out of `bd ready`.\n\n---\n\n## Filed from Graeme's drops, 2026-08-31 10:15 BST\n\nFour beads opened from things Graeme said in passing. Each is wired, none is\nstaffed, and the edges are why — not seat budget.\n\n| bead | what | held by |\n|---|---|---|\n| `bdi-7ao.26` | The TUI never clears on startup — **measured, independent of `.21`** | `bdi-7ao.21` (landing order in `Screen::showing`) |\n| `bdi-7ao.27` | `Snapshot::generated_at` says *now* for a project that was not re-read | `bdi-2bb.21` |\n| `bdi-7ao.28` | Run `/misplaced-emphasis` over `CLAUDE.md` | `bdi-n1m` |\n| `bdi-ev3` | **Spike:** embed a live herdr pane rather than redrawing its text | nothing — spawnable |\n\n**`bdi-7ao.27` is a published-contract defect, not a view nicety.**\n`generated_at` is in the JSON contract (`tests/snapshot_json.rs:121`), and\nprojects collect serially over `cfg.projects` filtered by `wanted` — so a\n`Wanted::Project` refresh leaves every skipped project untouched while\n`generated_at` is set to `now`. The Noctalia widget waits behind that contract,\nso a consumer would draw freshness wrong with no way to know. Wired behind\n`bdi-2bb.21` because that seat is settling view-wide vs per-project, which very\nlikely resolves it.\n\n**`bdi-ev3` is post-v1 and deliberately not parented here.** Graeme asked for a\nspike on embedding a real herdr panel, interactive if possible, *\"but if not,\nlive read-only would still be useful\"*. The useful finding already in the bead:\n`herdr agent read` takes `--format text|ansi` and `bdi` passes `text`\n(`src/collect/herdr.rs:50`), which is why it reads as content redrawn. The\nread-only half may be close to a format change plus an ANSI-to-spans parser.\n**The interactive half breaks a stated property** — `CLAUDE.md:4` says bdi is\nread-only, and sending keys into another agent's pane is a write. The spike\nsurfaces that for Graeme rather than deciding it.\n\n## Cost model for a collection, superseding the single number\n\nMeasured twice today. `93fb2c4` added a **second `bd` subprocess per project**\n(`bd query` for wisps, which `bd list` does not return), and it landed *after*\nthe first reading — so the two disagree for a reason:\n\n| | before `93fb2c4` | at `40f4eb5` |\n|---|---|---|\n| beady-eye alone | 0.61-0.70 s | 1.11-1.48 s |\n| summit-works alone | 2.02-2.48 s | 2.31-2.44 s |\n| both, as Graeme runs them | 2.49-2.71 s | 3.49-4.10 s |\n\nOnly the small tracker moved, which is the tell: cost is now substantially\n**per-project fixed**, not per-row. A config with four projects pays roughly\nfour times that regardless of size. `src/config.rs:87-95` still records\n9.4-11.2 s and is wrong; `bdi-2bb.21` is correcting it. `bdi-2bb.23` owns the\n30 s interval and now rests on a cost model with a per-project term.",
    "notes": "Ready and unstaffed at 17:37 BST 2026-08-30, held by seat budget rather than by any dependency: bdi-2bb.13 (P3, unblocks bdi-2bb.1), bdi-2bb.16 (P2), bdi-2bb.17 (P2), bdi-r5l (P3), bdi-7ao.11 (P3, a decision not an implementation). Five seats live on bdi-0bk, bdi-1pn, bdi-3d1, bdi-mse, bdi-qv1. Spawn from this list as those close; bdi-2bb.13 first, since bdi-2bb.1 is the design record and is the last thing blocking it.\nOperational, for any seat verifying its own push: 'gh run list --commit <sha>' returns [] for about a minute after a push while the commit filter catches up, even though the run exists. Matching on headSha from the unfiltered 'gh run list' finds it immediately. A seat that trusts the filtered query concludes its push triggered nothing. Measured by the bdi-6nm seat, 2026-08-30.\nIn `forest.rs`'s `finished()`, the conjunct `node.anomalies.is_empty()` is an unkillable mutant, and that is expected rather than a coverage gap. `anomaly::detect` raises an anomaly on a closed bead only when it carries an agent (StalePane); its other two rules require in_progress. So a closed bead with an anomaly and no agent cannot exist today, and `node.agent.is_none()` already excludes every case the anomaly check would. The `bdi-mse` seat hand-walked the mutants of 24a702b and kept the conjunct deliberately: dropping it would make `finished()` depend silently on an invariant that lives in `anomaly::detect`, so the first anomaly rule that fires on a closed unmanned bead would start collapsing warnings out of sight with nothing to catch it. A mutation-testing seat that reports it as a survivor should be told it is expected, not sent to chase it.\nHeld back deliberately at 08:45 BST 2026-08-31, by file contention rather than\nby any dependency. Each is spawnable the moment the seat named beside it lands.\nA cold orchestrator should read `agent_pane` from the tracker for who is live —\nthat is the process fact — and read this only for why the rest are not staffed.\n\n- `bdi-2bb.22` (blocker vs child drawn the same) — behind `bdi-2bb.25`, which\n  holds the same `src/view/` row and header code.\n- `bdi-sw4` (the brightest tier is the terminal default) — behind `bdi-2bb.25`\n  for the same files, and `bdi-2bb.25` changes the stated reason it rests on.\n  See the note on that bead.\n- `bdi-2bb.24` (a group the reader expanded shuts on refresh) — behind\n  `bdi-2bb.21`, which holds `src/tui.rs`.\n- `bdi-7ao.12` (dep-tree row shape survives only in fixtures) — behind all four\n  TUI/collect seats. It rewrites ~200 fixture rows across `view/forest.rs`,\n  `view/draw.rs`, `model/snapshot.rs`, `model/join.rs`, `app.rs`,\n  `tests/conflict_directions.rs` and `tests/credential_isolation.rs`. It\n  collides with every seat that touches a test, so it runs alone.\n- `bdi-7ao.13` — not held; folded into the `bdi-7ao.14` seat, which lands `-C`\n  and `direnv exec` together and then makes the tracker describe that one\n  deliverable once. `.13`'s docs/design.md bullet survives either way.\n\n`bdi-7ao.11` (cargo-mutants in the flake) is a Graeme decision, in his queue,\nnot held by contention.\nAmends the held-back note above, 08:50 BST 2026-08-31: **worktrees weaken the\ncontention argument, and seats moved into them this morning.**\n\n`bdi-2bb.25` is on branch `root-row-agent` in its own worktree, `bdi-7ao.14` on\n`wip/tracker-env` in its own, and `bdi-7ao.17` commits from the main checkout and\nthen removes its build sandbox. Measured cause, from the `bdi-7ao.17` seat: another\nseat's half-finished `src/view/phrase.rs` stopped the *shared* tree compiling, so\n`cargo test` there told it nothing about its own code. `bdi-2bb.25` ran `cargo test`\ntwice in the shared tree, got green, and that green was about four seats' work.\n\n**So the reason to hold a bead is no longer \"another seat holds that file\".** It is\nonly the landing order — whoever lands second rebases onto a rename or an argv\nchange they could not read. That is cheap for a mechanical change and expensive for\na semantic one, and the two seats sharing `src/collect/bd.rs` sorted it between\nthemselves in their own topics rather than needing the orchestrator.\n\nPractical effect for the next orchestrator: `bdi-2bb.22`, `bdi-2bb.24` and `bdi-sw4`\nare spawnable now if each seat takes a worktree, rather than waiting on `.25` and\n`.21` to land. `bdi-7ao.12` still wants to run alone — not for contention but because\nit rewrites ~200 fixture rows across seven files, so every seat that lands after it\nrebases onto a changed fixture shape.\n\nThe repo supports this: `.envrc` resolves `.env.local` and `.envrc.local` from\n`git rev-parse --git-common-dir`'s parent, so a worktree gets the tracker and the\ncredential. Checked from a worktree by the `bdi-2bb.25` seat, not assumed.",
    "status": "open",
    "priority": 1,
    "issue_type": "epic",
    "owner": "80714+GraemeF@users.noreply.github.com",
    "created_at": "2026-08-30T12:21:14Z",
    "created_by": "Graeme Foster",
    "updated_at": "2026-08-31T08:45:44Z",
    "metadata": {
      "agent_pane": "wD6:p1"
    },
    "dependency_count": 0,
    "dependent_count": 0,
    "comment_count": 0
  },
  {
    "id": "bdi-7ao.30",
    "title": "CLAUDE.md does not say what the push gate shows when you are merely behind",
    "description": "`CLAUDE.md` documents the `git reset --soft origin/main` trap and tells a seat to\nread `git diff origin/main HEAD --stat` before pushing. It does not say what that\ndiff looks like when you are merely **behind**, and that reading is the one a\nseat is most likely to misread as its own fault.\n\nWhen `origin/main` has moved and you have not fetched, the pre-push `--stat`\nlists the other seat's landed files as **deletions in your commit**:\n\n    Cargo.lock                            |   2 -\n    Cargo.toml                            |   7 -\n    src/tui.rs                            | 117 ---\n    tests/signal_restores_the_terminal.rs | 264 ---\n\nThat reads exactly like \"my change reverts a peer's work\". It is not — it is the\nsignature of a stale ref, and the fix is `git fetch origin` and re-read, not\ninvestigation.\n\n## The two failures share one diffstat, and that is the point\n\nThe `reset --soft` trap already in the file produces **the same shape**. Squash\nonto a moved `origin/main` and your index lacks the files that landed since, so\nyour commit genuinely deletes them and the inversions are real. Being behind\nproduces inversions that are an artefact of a stale ref. **A reader cannot tell\nthem apart from the diffstat.**\n\nSo the signature is worth recognising but is not the discriminator. **The\ndiscriminator is what survives a fresh fetch and rebase**: in the behind case the\ninversions vanish; in the revert case they are still there, and that is the\n`reset --soft` failure the file already warns about.\n\nAn earlier draft of this bead said the tell was \"every inverted file is theirs\nand none is yours\". That is wrong — it holds in both cases. Established by the\n`bdi-7ao.30` seat at cf3e11b before writing anything.\n\n## Why it earns a line\n\nThree encounters on 2026-08-31 alone:\n\n- The concierge mapped the fleet's file claims, read `origin/main` as unmoved,\n  and told the `bdi-7ao.21` seat it need not re-run its gate before pushing.\n  Wrong: the map was a measurement, the conclusion was a prediction about other\n  agents. Withdrawn.\n- `bdi-7ao.21` re-ran the gate anyway and pushed `79d061c`, which immediately put\n  `bdi-7ao.28` behind.\n- `bdi-7ao.28` hit `.21`'s push on its own freshness check and rebased cleanly,\n  then said it would not have recognised the signature had it checked in the\n  other order.\n\n## The rule to write\n\nBeside the existing squash paragraph, in the section `bdi-7ao.28` named\n`Getting a change landed`, rule first and incident second:\n\n- Fetch immediately before the push, not before the test run. A gate is a\n  measurement of the moment you push, and a reading taken before a long job does\n  not describe the state after it.\n- If the `--stat` lists files you did not write, you are probably behind rather\n  than wrong. Fetch, rebase, re-read — what survives that is a real revert. A\n  rejected push is the gate working.\n\nNot a second copy of the squash paragraph. It ties back to it, because the two\nare the same reading with different causes.\n\n## Done when\n\n`CLAUDE.md` carries the behind-signature alongside the `reset --soft` trap, names\nthe fetch-and-rebase as what separates them, and `nix flake check` is green on\nthe committed tree.",
    "status": "in_progress",
    "priority": 2,
    "issue_type": "task",
    "assignee": "Graeme Foster",
    "owner": "80714+GraemeF@users.noreply.github.com",
    "created_at": "2026-08-31T08:58:36Z",
    "created_by": "Graeme Foster",
    "updated_at": "2026-08-31T09:03:43Z",
    "started_at": "2026-08-31T09:02:34Z",
    "metadata": {
      "agent_pane": "wD6:pH",
      "working_topic": "beady-eye/v1-bdi-7ao.30"
    },
    "dependencies": [
      {
        "issue_id": "bdi-7ao.30",
        "depends_on_id": "bdi-7ao",
        "type": "parent-child",
        "created_at": "2026-08-31T08:58:36Z",
        "created_by": "Graeme Foster",
        "metadata": "{}"
      }
    ],
    "dependency_count": 0,
    "dependent_count": 0,
    "comment_count": 0,
    "parent": "bdi-7ao"
  }
]

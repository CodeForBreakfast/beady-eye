# rust
/target/
/mutants.out/
/mutants.out.old/

# nix
result
result-*
.direnv/

# beads: credentials never leave the machine
.env.local
.beads-credential-key

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads/proxieddb/
